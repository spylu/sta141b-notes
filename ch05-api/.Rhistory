username <- "randy3k"
r <- GET(
str_glue("https://api.github.com/users/{username}/repos"),
add_headers(Authorization = paste("token", Sys.getenv("GITHUB_PAT")))
)
library(tidyverse)
library(jsonlite)
library(lubridate)
username <- "randy3k"
r <- GET(
str_glue("https://api.github.com/users/{username}/repos"),
add_headers(Authorization = paste("token", Sys.getenv("GITHUB_PAT")))
)
username <- "randy3k"
# need this `GET` parameter to go to next page
r <- GET(
str_glue("https://api.github.com/users/{username}/repos"),
add_headers(Authorization = paste("token", Sys.getenv("GITHUB_PAT")))
)
r <- GET(
"https://content.guardianapis.com/search",
query = list(
`api-key` = Sys.getenv("GUARDIAN_KEY"),
q = "coronavirus"
)
)
